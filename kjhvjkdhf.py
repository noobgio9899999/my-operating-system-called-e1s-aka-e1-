import pygame
a=input("do you want to run this say yes if you do want to ")
if a=="yes":
  pygame.init()
  screen = pygame.display.set_mode((560, 450))
  running = True
  pygame.display.set_caption("pi fun")
  while running:
    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            running = False
  print("download vscode")
else:
  print("ok")
c=input("search")
b=input(pygame.display.set_caption(c))
if b==input(pygame.display.set_caption(" fun games")):
    math=input("whats 1+1")
    if math==" 2":
     print("correct")
    else:
     print("wrong")
    pygame.init()
    screen = pygame.display.set_mode((560, 450))
    running = True
    pygame.display.set_caption("mpfun")
    while running:
     for event in pygame.event.get():
        if event.type == pygame.QUIT:
            running = False
    pygame.display.set_caption("play with burger")
    pygame.display.set_caption(" | (  .        . ) |")
    pygame.display.set_caption(" | (       U     ) |")
    pygame.display.set_caption(" | --|____________|-----------------------------------------------------------------🍔---|")
    pygame.display.set_caption ("|   |           ||")
    controls=input("control your player with w a s d")
    if controls:
     pygame.display.set_caption("w")
    if controls=="w":
     pygame.display.set_caption(" ( .   .)")
     pygame.display.set_caption(" (   U  )")
     pygame.display.set_caption(" -|_____|-🍔")
     pygame.display.set_caption("  |     |")
import time
#ling gang guli guli gu le watcha lin gang gu ling gang goo
print("do not close the tab"+"...UNLESS YOU HAVE PLAYED IT FOR MORE THAN 86405.6 SECONDS")
if running==True:
 time.sleep(86405)
 pygame.display.set_caption("brainrot detected, if you find this annoying get out")
 time.sleep(0.6)
 pygame.QUIT